#!/bin/sh

# set -x

tmpdir="$(dirname "$(mktemp -u)")"

nvim --cmd "autocmd VimEnter * noautocmd windo \
    if expand('%:p') =~# '^\\V' . '${tmpdir}' | \
        setlocal nomodifiable | endif" "${@}"
