#!/bin/sh

tmpdir="$(dirname "$(mktemp -u)")"

echo "noautocmd argdo \
    if expand('%:p') =~# '^\\V' . '${tmpdir}' | \
        setlocal nomodifiable | \
    endif | \
    b1 | \
    exec 'normal! gg' |  call histdel('cmd','^noautocmd argdo') | visual" |  nvim -e "${@}"
